version: '3.9'

services:
  django:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env.dev
    command: python manage.py runserver 0.0.0.0:8000
